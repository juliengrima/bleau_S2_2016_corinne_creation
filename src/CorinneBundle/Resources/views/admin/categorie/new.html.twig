{% extends '@Corinne/admin/index.html.twig' %}


{% block body %}
    <div class="container">
        <div class="card teal center">
            <h1 class="white-text">Ajouter une categorie :</h1>
        </div>

        {{ form_start(form) }}
        <center>
        <div class="reducto">
            <div class="row left-align">
                <div class="col s12">
                    <label><h6>Nom de la catégorie</h6></label>
                    {{ form_widget(form.nomcat) }}
                    {{ form_errors(form.nomcat) }}
                </div>
            </div>
            <div class="row left-align">
                <div class="col s12">
                    <label><h6>Image de la categorie</h6></label>
                    {{ form_widget(form.source) }}
                    {{ form_errors(form.source) }}
                </div>
            </div>
            <div class="row left-align">
                <div class="col s12">
                <label><h6>Nom de l'image</h6></label>
                {{ form_widget(form.alt) }}
                {{ form_errors(form.alt) }}
            </div>
        </div>
        </center>
            <div class="row center">
                <ul>
                    <li>
                        <input class="waves-effect waves-light btn" type="submit" value="Valider" />
                        <a href="{{ path('categorie_index') }}" class="waves-effect waves-light btn">Retour a la Liste</a>
                    </li>
                </ul>
            </div>

        {{ form_end(form) }}

    </div>
{% endblock %}
