{% extends '@Corinne/admin/index.html.twig' %}

{% block body %}
<div class="container">
    <div class="card teal center">
    <h1 class="white-text">Ecolabel :</h1>
    </div>
    <table class="bordered  highlight center responsive-table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ecolabel.id }}</td>
            </tr>
            <tr>
                <th>Source</th>
                <td>{{ ecolabel.source }}</td>
            </tr>
            <tr>
                <th>Alt</th>
                <td>{{ ecolabel.alt }}</td>
            </tr>
            <tr>
                <th>Texte</th>
                <td>{{ ecolabel.texte }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('ecolabel_index') }}" class="waves-effect waves-light btn" style="width:220px">Retour a la liste</a>
        </li>
        <li>
            <a href="{{ path('ecolabel_edit', { 'id': ecolabel.id }) }}" class="waves-effect waves-light btn" style="width:220px">Editer</a>
        </li>
        {#<li>#}
            {#{{ form_start(delete_form) }}#}
                {#<input type="submit" value="Delete">#}
            {#{{ form_end(delete_form) }}#}
        {#</li>#}
    </ul>
</div>
{% endblock %}
