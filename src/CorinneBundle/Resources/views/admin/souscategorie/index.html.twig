{% extends '@Corinne/admin/index.html.twig' %}

{% block body %}

    <div class="container">


        <div class="row">
            <div class="col s12 m12 l12">
                <div class="card teal center">
                    <h1 class="white-text">Sous-categories :</h1>
                </div>
            </div>
        </div>

        <div class="row center">
            <div class="col s12">
                <a href="{{ path('souscategorie_new') }}" class="waves-effect waves-light btn">Nouvelle sous catégorie</a>
            </div>
        </div>

        <table class="highlight centered">
            <thead> <!-- En-tête du tableau -->
            <tr>
                <th>Catéorie</th>
                <th>Sous categorie</th>
                <th>Action</th>
            </tr>
            </thead>

            <tbody> <!-- Corps du tableau -->
            {% for sousCategorie in sousCategories %}
                <tr>
                    {#<td></td>#}
                    <td>{{ sousCategorie.nomsouscat }}</td>
                    <td>
                        <ul>
                            <li>
                                <a href="{{ path('souscategorie_edit', { 'id': sousCategorie.id }) }}">Editer</a>
                            </li>
                            <li>
                                <a href="{{ path('souscategorie_delete', { 'id': sousCategorie.id }) }}">Supprimer</a>
                            </li>
                        </ul>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

{% endblock %}
