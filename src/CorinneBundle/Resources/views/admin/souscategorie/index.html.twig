{% extends '@Corinne/admin/index.html.twig' %}

{% block body %}

    <div class="container">

        <div class="row">
            <div class="col s12 m12 l12">
                <div class="card teal center">
                    <h1 class="white-text">Listes des Categories :</h1>
                </div>
            </div>
        </div>

        <div class="row center">
            <div class="col s12 m6 l6">
                <a href="{{ path('categorie_new') }}" class="waves-effect waves-light btn" style="width:280px">Nouvelle catégorie</a>
            </div>
            <div class="col s12 m6 l6">
                <a href="{{ path('menu_categorie') }}" class="waves-effect waves-light btn" style="width:280px">Retour
                    au menu</a>
            </div>
        </div>

        <div class="row center">
            <div class="col s12 m6 l3">
            </div>
            <div class="col s12 m6 l3">
                <p>Numéro de catégorie</p>
            </div>
            <div class="col s12 m6 l3">
                <p>Catégorie</p>
            </div>
            <div class="col s12 m6 l3">
            </div>
        </div>

        {% for categorie in categories %}
            <div class="row center">
                <div class="col s12 m6 l3">
                </div>
                <a href="{{ path('souscategorie_edit', { 'id': sousCategorie.id }) }}">
                    <div class="e">
                        <div class="col s12 m6 l3 f">
                            {{ categorie.id }}
                        </div>
                        <div class="col s12 m6 l3 f">
                            {{ categorie.nomcat }}
                        </div>
                    </div>
                </a>
                <div class="col s12 m6 l3">
                </div>
            </div>
        {% endfor %}

    </div>













<div class="container">
    <div class="card teal center">
    <h1 class="white-text">Liste SousCategorie :</h1>
    </div>
    <table class="bordered highlight centered responsive-table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nomsouscat</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for sousCategorie in sousCategories %}
            <tr>
                <td><a href="{{ path('souscategorie_show', { 'id': sousCategorie.id }) }}">{{ sousCategorie.id }}</a></td>
                <td>{{ sousCategorie.nomsouscat }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('souscategorie_show', { 'id': sousCategorie.id }) }}" class="waves-effect waves-light btn" style="width:100px">voir</a>
                        </li>
                        <li>
                            <a href="{{ path('souscategorie_edit', { 'id': sousCategorie.id }) }}" class="waves-effect waves-light btn" style="width:100px">editer</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('souscategorie_new') }}" class="waves-effect waves-light btn" style="width:320px">Ajouter
                une sous categorie</a>
        </li>
    </ul>
    <ul>
        <li>
            <a href="{{ path('menu_categorie') }}" class="waves-effect waves-light btn" style="width:320px">Retour
                au menu</a>
        </li>
    </ul>
</div>
{% endblock %}
